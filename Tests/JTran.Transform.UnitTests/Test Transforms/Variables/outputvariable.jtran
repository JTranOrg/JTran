{
  "#variable(Company)":       "#(Company)",
  "#outputvariable(Company)": "#(Company)",
 
  "#foreach(Customers, Customers)":
  {
    "Company":    "#($Company)",
    "#noobject":  "#copyof(@)"
  }
}
